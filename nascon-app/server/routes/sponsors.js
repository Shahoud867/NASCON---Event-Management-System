const express = require('express');
const router = express.Router();

// Placeholder for sponsors routes
router.get('/', (req, res) => {
  res.json({ message: 'Sponsors routes - Phase 3 implementation' });
});

module.exports = router;
