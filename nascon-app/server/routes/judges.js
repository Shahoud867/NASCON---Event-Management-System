const express = require('express');
const router = express.Router();

// Placeholder for judges routes
router.get('/', (req, res) => {
  res.json({ message: 'Judges routes - Phase 4 implementation' });
});

module.exports = router;
