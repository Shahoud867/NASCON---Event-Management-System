const express = require('express');
const router = express.Router();

// Placeholder for accommodations routes
router.get('/', (req, res) => {
  res.json({ message: 'Accommodations routes - Phase 4 implementation' });
});

module.exports = router;
